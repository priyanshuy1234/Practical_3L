<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Edit Game</title>
</head>
<body>
    <%- include('../partials/header') %>
    <div class="container">
        <h1>Edit Game</h1>
        <form action="/games/<%= game._id %>?_method=PUT" method="POST" class="needs-validation" novalidate>
            <div class="form-group">
                <label for="gameName">Game Name</label>
                <input type="text" class="form-control" id="gameName" name="gameName" value="<%= game.gameName %>" readonly required>
                <div class="invalid-feedback">
                    Game name cannot be changed.
                </div>
            </div>
            <div class="form-group">
                <label for="numberOfPlayer">Number of Players</label>
                <input type="number" class="form-control" id="numberOfPlayer" name="numberOfPlayer" value="<%= game.numberOfPlayer %>" required>
                <div class="invalid-feedback">
                    Please provide a valid number of players.
                </div>
            </div>
            <div class="form-group">
                <label for="gameDuration">Game Duration</label>
                <input type="text" class="form-control" id="gameDuration" name="gameDuration" value="<%= game.gameDuration %>" required>
                <div class="invalid-feedback">
                    Please provide a valid game duration.
                </div>
            </div>
            <div class="form-group">
                <label for="platform">Platform</label>
                <input type="text" class="form-control" id="platform" name="platform" value="<%= game.platform %>" required>
                <div class="invalid-feedback">
                    Please provide a valid platform.
                </div>
            </div>
            <button type="submit" class="btn btn-primary">Update Game</button>
        </form>
    </div>
    <%- include('../partials/footer') %>
    <script>
        // Example of Bootstrap validation
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')
            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }
                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
</body>
</html>